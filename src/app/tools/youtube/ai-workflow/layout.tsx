import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'AI YouTube Automation Workflow: $0 Scaling System',
  description: 'Design a full $0 cost YouTube automation system. The exact workflow to scale your channel with AI.',
  keywords: ['YouTube automation workflow', 'AI content system', 'low cost YouTube'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
